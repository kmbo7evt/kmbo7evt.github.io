# POST
## Flag:flag{1f8909609f0f666461a0780d0d44d9a8}
## 解题过程：
进入场景后发现与Get相似，尝试使用？what=flag来找flag，出现失败。明明What参数就在眼前却无法找到flag。通过Ai询问后发现这题无法像上一题一样直接读取连接后的GET内容，而只读取POST所以无法照猫画狗。AI建议我建立一个html文档然后输入他给的代码，运行后成功提取flag。代码中的from method=post来以post形式发送数据，input name...value来将what与flag串联，acton来确认网址。
## 困难点：
1.发现旧方法不管用，无法直接利用GET。2.Ai给的代码无法理解，通过询问理解原理。
## 感想：
Ai给的方法有时候不会直接讲解原理，若想要真正的学习还需深入询问。一开始无脑复制代码得到flag时我也想过直接换到下一题，但是感觉这个方法要是我绝对想不出来，于是深入学习。
